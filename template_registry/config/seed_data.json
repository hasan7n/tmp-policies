{
    "policy_templates": [
        {
            "name": "download",
            "policy_data_schema": {
                "allowed_institutions": ["list of strings"],
                "consent_document": "string"
            }
        }
    ],
    "credential_templates": [
        {"template_type": "membership", "claims_schema": {"member_of": "string"}},
        {"template_type": "consent",    "claims_schema": {"document":  "string"}},
        {"template_type": "public_key", "claims_schema": {"key":       "string"}}
    ]
}
