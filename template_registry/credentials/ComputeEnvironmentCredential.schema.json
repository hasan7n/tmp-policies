{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ComputeEnvironmentCredential",
  "title": "ComputeEnvironmentCredential",
  "description": "The assignee's compute node meets technical requirements; issuer claims the subject meets the technical requirements.",
  "x-subject": "A Compute Node",
  "x-issuer": "Compute Provider (e.g. Flower, FedBioMed)",
  "type": "object",
  "properties": {
    "hasComputeProfile": {
      "type": "object",
      "description": "The compute profile describing the technical capabilities of the compute node.",
      "properties": {
        "profile": {
          "type": "object",
          "description": "Technical profile of the compute node. Claims are TBD.",
          "additionalProperties": true
        }
      },
      "required": ["profile"],
      "additionalProperties": false
    }
  },
  "required": ["hasComputeProfile"],
  "additionalProperties": false
}
