{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "AgreementCredential",
  "title": "AgreementCredential",
  "description": "The assignee has agreed to a specific document; issuer claims the subject agreed to a document of a given version.",
  "x-subject": "A user requesting access to data",
  "x-issuer": "Sage / User Platform",
  "type": "object",
  "properties": {
    "agreementInfo": {
      "type": "object",
      "description": "Details identifying the document the subject agreed to.",
      "properties": {
        "documentID": {
          "type": "string",
          "description": "Identifier of the agreed document."
        },
        "documentVersion": {
          "type": "string",
          "description": "Version of the agreed document; may be the document hash."
        }
      },
      "required": ["documentID", "documentVersion"],
      "additionalProperties": false
    }
  },
  "required": ["agreementInfo"],
  "additionalProperties": false
}
