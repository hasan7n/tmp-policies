{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ScopedAgreementCredential",
  "title": "ScopedAgreementCredential",
  "description": "The document is identified by an obligation ontology, a terms author (counterParty), and a project+dataset scope.",
  "x-subject": "An agreement document",
  "x-issuer": "Sage / User Platform",
  "type": "object",
  "properties": {
    "agreementInfo": {
      "type": "object",
      "description": "Details identifying the agreement document, its counter party, and scope.",
      "properties": {
        "counterParty": {
          "type": "string",
          "description": "DID of the terms author (counter party)."
        },
        "scope": {
          "type": "object",
          "description": "Scope of the agreement, defined by obligation, project, and dataset.",
          "properties": {
            "obligation": {
              "type": "string",
              "description": "Obligation ontology term (e.g. Collaboration)."
            },
            "project": {
              "type": "string",
              "description": "DID of the project the agreement applies to."
            },
            "dataset": {
              "type": "string",
              "description": "DID of the dataset the agreement applies to."
            }
          },
          "required": ["obligation", "project", "dataset"],
          "additionalProperties": false
        }
      },
      "required": ["counterParty", "scope"],
      "additionalProperties": false
    }
  },
  "required": ["agreementInfo"],
  "additionalProperties": false
}
